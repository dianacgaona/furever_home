{/* {this.state.profileUser === this.props.currentUser ? <AddPost getPosts={this.getPosts} />
  <ProfileModal /> : null} */}


  {this.state.profileUser.id === this.props.currentUser.id ? (
    <div>
    <AddPost getPosts={this.getPosts} />
    <ProfileModal />
    </div>
  ) : (
     null
  )}
